<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- BASE THEME -->
    <style name="Base.Theme.PetFect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores globales si quieres -->
    <item name="colorPrimary">@color/rosita_fondoss_tarjetas_alertaslev</item>
    <item name="colorSecondary">@color/rosita_fondoss_tarjetas_alertaslev</item>
  </style>

  <!-- TU TEMA PRINCIPAL -->
    <style name="Theme.PetFect" parent="Base.Theme.PetFect">
        <!-- Si quieres puedes añadir cosas globales aquí -->
    </style>

    <!-- ESTILOS REUTILIZABLES -->
    <style name="TituloLegal">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">12dp</item>
    </style>

    <style name="TextLegal">
        <item name="android:textSize">14sp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">4dp</item>
    </style>

</resources>