<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Cámara opcional -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- Android <= 12 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/petfect"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/petfect_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetFect">

        <activity
            android:name=".splash.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".auth.Login" android:exported="false" />
        <activity android:name=".auth.SignUp" android:exported="false" />
        <activity android:name=".main.MainActivity" android:exported="false" />
        <activity android:name=".main.Profile" android:exported="false" />
        <activity android:name=".main.PreferenciasActivity" android:exported="false" />
        <activity android:name=".main.PublicacionActivity" android:exported="false" />
        <activity android:name=".main.PerdidosActivity" android:exported="false" />
        <activity android:name=".main.AdopcionesActivity" android:exported="false" />
        <activity android:name=".main.MapaActivity" android:exported="false" />
        <activity android:name=".main.ActivityTarjeta" android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>