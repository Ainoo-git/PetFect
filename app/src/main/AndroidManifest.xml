<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permisos -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/petfect"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/petfect_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetFect">
        <activity
            android:name=".configuracion.ConfiguracionActivity"
            android:exported="false" />
        <activity
            android:name=".main.MapaActivity"
            android:exported="false" /> <!-- Splash -->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB782JpfbeNrzMS2igviCaPWLLuFlymHAM" />
        <!-- Splash -->
        <activity
            android:name=".splash.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Auth -->
        <activity
            android:name=".auth.Login"
            android:exported="false" />
        <activity
            android:name=".MainBab"
            android:exported="false" />
        <activity
            android:name=".auth.SignUp"
            android:exported="false" /> <!-- Main -->
        <activity
            android:name=".main.MainActivity"
            android:exported="false" /> <!-- Perfil -->
        <activity
            android:name=".main.Profile"
            android:exported="false" /> <!-- Preferencias -->
        <activity
            android:name=".main.PreferenciasActivity"
            android:exported="false" /> <!-- Preferencias -->
        <activity
            android:name=".main.PerdidosActivity"
            android:exported="false" /> <!-- Perdidos -->
        <activity
            android:name=".main.AdopcionesActivity"
            android:exported="false" /> <!-- Adopciones -->
        <activity
            android:name=".main.PublicacionActivity"
            android:exported="false" />
        <activity
            android:name=".main.ActivityTarjeta"
            android:exported="false" />
        <activity
            android:name=".creditos.Creditos"
            android:exported="false"/>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>